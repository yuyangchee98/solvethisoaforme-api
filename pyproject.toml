[project]
name = "patent-claim-nlp-api"
version = "0.1.0"
description = "Patent Claim NLP API for antecedent basis checking"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "spacy>=3.8.0",
    "python-docx>=1.1.0",
]

[tool.uv]
dev-dependencies = []

[tool.uv.sources]
